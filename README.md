# Assignment 6 – University Courses API

This project is an Express.js and SQLite backend application that manages a university's course catalog. It allows users to view, add, update, and delete courses through RESTful API endpoints.

---

## Overview

The Computer Science department needed a better way to manage course information instead of using spreadsheets and paper forms. This API provides a modern solution that stores data in a SQLite database and uses Express.js to handle all CRUD operations.

---

## Project Structure
